{
  "version": 1,
  "name": "performance_showcase",
  "description": "A high-density live-data plot. Judge only content inside the plot; ignore metric cards, telemetry, headings, buttons, controls, side panels, and other surrounding UI.",
  "expected_image": {
    "width": 1400,
    "height": 900,
    "minimum_file_size_bytes": 50000
  },
  "policy": {
    "blocking_severities": ["high", "medium"],
    "minimum_confidence": 0.8
  },
  "checks": [
    {
      "id": "overall_layout",
      "severity": "high",
      "expectation": "One coherent high-density plot is visible. Its axes, grid, and curve stay within the plot area without unintended overlap or clipping. Do not evaluate metric cards, telemetry, controls, side panels, or other content outside the plot."
    },
    {
      "id": "dense_curve",
      "severity": "high",
      "expectation": "A continuous high-density oscillating curve spans the Sample domain plot between approximately -10 and 10 Amplitude. Repeated peaks and troughs are visible rather than a blank plot or solid block."
    },
    {
      "id": "text_raster_quality",
      "severity": "high",
      "expectation": "Within the plot, axis titles and tick labels are readable and free of clear raster corruption. Fail only for concrete defects such as square pixel blocks, doubled glyphs, or severe stair-step edges that make plot text materially illegible. Do not compare plot text with metric cards, telemetry, headings, buttons, controls, or any text outside the plot. Normal antialiasing, small fonts, rotated text, and backend-specific softness are acceptable."
    },
    {
      "id": "rendering_integrity",
      "severity": "high",
      "expectation": "The dense curve remains distinguishable from the grid, and the axes and plot text are not corrupted or materially clipped."
    }
  ]
}
