#
# SPDX-FileCopyrightText: 2026 Michal Grabarczyk
# SPDX-License-Identifier: GPL-3.0-only WITH Universal-FOSS-exception-1.0
#
# This file is also available under a separate commercial license.
# See COMMERCIAL-LICENSING.md for contact information.
#
name: Build, examples, and unit tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-test:
    name: ${{ matrix.compiler }} / Qt private API ${{ matrix.qt_private_api }}
    # Pinned so the preinstalled Clang version stays fixed.
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        compiler: [gcc]
        qt_private_api: ['ON', 'OFF']
        include:
          - compiler: gcc
            cxx: g++
          # Clang against libstdc++, which the aqt gcc_64 Qt binaries require.
          # Warnings are reported but not fatal until the build is warning-clean.
          - compiler: clang
            qt_private_api: 'ON'
            cxx: clang++-18
            cxx_flags: -Wall -Wextra

    steps:
      - uses: actions/checkout@v6

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes build-essential cmake ninja-build xvfb libgl1-mesa-dev libxcb-cursor0 libxkbcommon-x11-0

      - name: Install Qt
        uses: jurplel/install-qt-action@v4.3.1
        with:
          version: '6.7.3'
          target: desktop
          modules: qtshadertools
          cache: true
          setup-python: false

      - name: Configure CMake
        run: >-
          cmake -S . -B build -G Ninja
          -DCMAKE_BUILD_TYPE=Release
          -DCMAKE_CXX_COMPILER=${{ matrix.cxx }}
          "-DCMAKE_CXX_FLAGS=${{ matrix.cxx_flags }}"
          -DQACCELPLOT_BUILD_TESTS=ON
          -DQACCELPLOT_BUILD_EXAMPLES=ON
          -DQACCELPLOT_BUILD_BENCHMARKS=OFF
          -DQACCELPLOT_INSTALL=ON
          -DQACCELPLOT_USE_QT_PRIVATE_API=${{ matrix.qt_private_api }}

      - name: Build library, examples, and unit tests
        run: >-
          cmake --build build --config Release --parallel 2 --target
          QAccelPlotExamples
          QAccelPlotTests

      - name: Run unit tests
        run: >-
          xvfb-run -a ctest --test-dir build -C Release --output-on-failure
          -L '^unit-test$'
